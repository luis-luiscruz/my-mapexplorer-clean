# Minimal requirements for Docker - only packages actually used
selenium>=4.15.0
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=4.9.0
python-dotenv>=1.0.0

# Chrome WebDriver management (only for Windows development)
# webdriver-manager>=4.0.0  # Commented out for Docker - using system Chrome

# Optional but useful
fake-useragent>=1.4.0
urllib3>=2.0.0
